# ---- personal data: never publish ----
# personal profile (targets, salary band)
config.json
# candidate-scoring profile: target employers, comp band, skills, exclusions
# (the glob also catches versioned backups like profile.json.v1.bak-*)
profile.json
profile.json.*
# personal resume / scoring calibration
RUBRIC.md
# personal targeting narrative
WORKFLOW.md
# personal build plans (salary band, target companies)
plans/
# personal shortlists
picks.json
picks_llm.json
# scraped jobs, scored CSVs, tracker .xlsx, all caches
results/
*.xlsx
~$*.xlsx

# ---- environment / generated ----
# local agent session/job state
.claude/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/
.DS_Store
